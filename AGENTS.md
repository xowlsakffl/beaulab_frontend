# 프론트엔드 작업 안내

## 확인 순서

- `README.md`는 프로젝트 소개이며, 실제 구현과 계약은 현재 소스가 기준이다.
- 루트와 대상 앱의 `package.json`, Next 설정, 가까운 기존 화면의 Client·components·hooks·lib를 함께 확인한다.
- 개발 도구 버전과 실행 명령은 각 workspace의 설정을 따른다.

## 작업 기준

- `app`은 라우트와 화면 연결, `components`는 UI, `hooks`는 React 상태·수명, `lib`는 타입·변환·검증·API 로직을 담당한다.
- staff의 `common`은 직원 앱 공통이다. 도메인 필드·endpoint·권한에 묶인 코드는 해당 앱·도메인에 둔다.
- 앱 간 직접 import, 패키지에서 앱 import, 패키지 src 우회 import를 하지 않는다. `@beaulab/*`의 공개 export를 사용한다.
- 기존 UI·폼·목록·캐시·미디어 패턴을 우선 재사용한다. 줄 수만 줄이려는 과도한 분리·공통화는 피한다.
- 폼은 의미 있는 섹션 단위로 구성하고 한 줄 입력 높이는 기본 `h-11`을 사용한다.
- 새 직원 경로는 권한 매핑과 Guard를 확인한다. 프론트 권한 UI는 서버 인가를 대신하지 않는다.
- API 요청은 앱 wrapper로 연결한다. 조회 취소·오류·중복 요청과 세션 변경에 따른 캐시 수명을 보존한다.
- 비밀번호, 인증번호, 토큰, 쿠키와 개인정보 응답을 console이나 문서에 기록하지 않는다.
- 기존 미커밋 변경을 보존하고 요청과 무관한 코드·설정·의존성 변경을 피한다.

## 검증

- 변경 파일만 Prettier로 정리하고 대상 앱의 lint·typecheck를 실행한다.
- 공유 패키지 변경은 사용하는 앱들의 타입과 빌드 영향도 확인한다.
- 브라우저에서 로딩·오류·입력·선택·파일 교체·화면 이동과 좁은 화면의 넘침을 확인한다.
- 실제 환불·발송·계정 변경을 검증용으로 임의 실행하지 않는다. 실행하지 못한 검사는 명시한다.

## 문서와 원격 저장소

- `xowlsakffl`에는 `README.md`, `AGENTS.md`만 유지하고 상세 개발·운영 문서는 포함하지 않는다.
- `origin`과 `xowlsakffl`의 README와 문서 구성을 섞지 않는다. 동기화 전후에 커밋의 파일 목록을 확인한다.
- 로컬 절대 경로나 접근할 수 없는 내부 문서에 작업 규칙을 의존시키지 않는다.
